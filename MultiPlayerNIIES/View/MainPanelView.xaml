<UserControl x:Class="MultiPlayerNIIES.View.MainPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MultiPlayerNIIES.View"
             mc:Ignorable="d" 
              d:DesignWidth="800">
    <Grid>
        <ToolBar Height="40" VerticalAlignment="Top">
            <Separator />
            <Button Command="{Binding OpenCommand}">
                <Image Source="/MultiPlayerNIIES;component/Images/PNG/Page-White-Add.png" />
            </Button>
            <Button Command="{Binding SaveCommand}">
                <Image Source="/MultiPlayerNIIES;component/Images/PNG/Disk.png" />
            </Button>


            <Button  Command="{Binding ImportCommand}">
                <Image Source="/MultiPlayerNIIES;component/Images/PNG/Page-White-Put.png" />
            </Button>

            <Separator />
            <Button Command="{Binding AddCommand}">
                <Image Source="/MultiPlayerNIIES;component/Images/PNG/Add.png" />
            </Button>

            <Button  Command="{Binding RemoveCommand}" >
                <Image Source="/MultiPlayerNIIES;component/Images/PNG/Delete.png" />
            </Button>
            <Separator/>


            <Button  Command="{Binding MeasureCommand}">
                <Image Source="/MultiPlayerNIIES;component/Images/PNG/Measure.png" />
            </Button>
            <Separator />
            <TextBox Foreground="LightGray" Width="100">Поиск по имени...</TextBox>
        </ToolBar>

    </Grid>
</UserControl>
