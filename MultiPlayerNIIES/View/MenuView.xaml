<UserControl x:Class="MultiPlayerNIIES.View.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MultiPlayerNIIES.View"
             mc:Ignorable="d" 
              d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <!--<Rectangle 
            x:Name="Dragger" 
            Fill="#66000000" 
            Panel.ZIndex="100" 
            Margin="300 0 150 0 "
            Cursor="SizeAll"/>-->
        <Menu 
              Grid.Column="0"
              BorderBrush="#FF111111"
              BorderThickness="0.5" 
              Foreground="#FF383838"  >
                <Menu.Background>
                <LinearGradientBrush EndPoint="0.5,0" StartPoint="0.5,0.6">
                    <GradientStop Color="#FFDAAB6B" Offset="0"/>
                    <GradientStop Color="#FFD0974E" Offset="1"/>
                </LinearGradientBrush>
            </Menu.Background>
            <MenuItem Header="_Файл">

                <MenuItem Header="_Открыть видео" Command="{Binding OpenCommand}" >
                    <MenuItem.Icon>
                        <Image Source="/MultiPlayerNIIES;component/Images/PNG/Gnome-Media-Eject-64.png" Stretch="Fill" Width="20"/>
                    </MenuItem.Icon>
                </MenuItem>
                <Separator />
                <MenuItem Header="_Открыть состояние плеера" Command="{Binding OpenStateCommand}"  >
                    <MenuItem.Icon>
                        <Image Source="/MultiPlayerNIIES;component/Images/PNG/Gnome-Folder-Open-64.png" Stretch="Fill" Width="20"/>
                    </MenuItem.Icon>
                </MenuItem>

                <MenuItem Header="_Сохранить состояние" Command="{Binding SaveStateCommand}">
                    <MenuItem.Icon>
                        <Image Source="/MultiPlayerNIIES;component/Images/PNG/Gnome-Media-Floppy-64.png" Stretch="Fill" Width="20"/>
                    </MenuItem.Icon>
                </MenuItem>


                <Separator />
                <MenuItem Header="_Закрыть все видеоокна" Command="{Binding СloseAllCommand}">
                    <MenuItem.Icon>
                        <Image Source="/MultiPlayerNIIES;component/Images/PNG/Gnome-Process-Stop-64.png" Stretch="Fill" Width="20"/>
                    </MenuItem.Icon>
                </MenuItem>

                <Separator />
                <MenuItem Header="_Выход" Command="{Binding CloseAppCommand}">
                    <MenuItem.Icon>
                        <Image Source="/MultiPlayerNIIES;component/Images/PNG/Cross.png" Width="15" Height="15"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Инструменты">
                <MenuItem Header="_Связь с Excel" Command="{Binding ExcelOpenCommand}">
                    <MenuItem.Icon>
                        <Image  Source="/MultiPlayerNIIES;component/Images/PNG/Ms-Excel-48.png" Stretch="Fill" Width="20"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
            <MenuItem Header="_Вид">
                <MenuItem Header="_Панель Видеополосы" Command="{Binding  StripesContainerShowHideCommand}">
                    <MenuItem.Icon>
                        <Image  Source="/MultiPlayerNIIES;component/Images/PNG/Gnome-Format-Justify-Fill-64.png" Stretch="Fill" Width="20"/>
                    </MenuItem.Icon>
                </MenuItem>
               
            </MenuItem>

            <MenuItem Header="_Опции">
                <MenuItem Header="_Настройки" Command="{Binding SettingsOpenCommand}">
                    <MenuItem.Icon>
                        <Image Source="/MultiPlayerNIIES;component/Images/PNG/Gnome-Applications-System-64.png" Stretch="Fill"  Width="20"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>

            <MenuItem Header="_Справка">
                <MenuItem Header="_О программе" Command="{Binding AboutCommand}">
                    <MenuItem.Icon>
                        <Image Source="/MultiPlayerNIIES;component/Images/PNG/Question.png" Stretch="Fill" Width="20"/>
                    </MenuItem.Icon>
                </MenuItem>
            </MenuItem>
        </Menu>
        
        <!--<StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button 
            Grid.Column="1" Width="40" Height="20"
            Margin="0"
                Command="{Binding MinimizeWindowCommand}"
            Style="{StaticResource OrangeWindowButtonStyle}">
                <Image Source="/MultiPlayerNIIES;component/Images/PNG/Minimize.png"></Image>
            </Button>
            <Button 
            Grid.Column="1" Width="40" Height="20" 
            Margin="0"
                Command="{Binding MaximizeRestoreWindowCommand}"
                Style="{StaticResource OrangeWindowButtonStyle}">
                <Image Source="/MultiPlayerNIIES;component/Images/PNG/Maximize.png"></Image>
            </Button>
        <Button 
            Grid.Column="1" Width="40" Height="20"
            Margin="0"
            Command="{Binding CloseAppCommand}"
            Style="{StaticResource OrangeWindowButtonStyle}">
                <Image Source="/MultiPlayerNIIES;component/Images/PNG/Close.png"></Image>
            </Button>
        </StackPanel>-->
    </Grid>
</UserControl>
